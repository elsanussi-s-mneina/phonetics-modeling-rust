# Phonetics Modeling (subproject implemented in Rust)
## Prerequisites:
  + A rust compiler
  - Cargo (the rust package manager)

## How to run:

(Browse to the subdirectory containing the cargo.toml file.)

Run the following command:

`cargo run`
